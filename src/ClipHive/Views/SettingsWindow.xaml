<Window x:Class="ClipHive.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:ClipHive.Helpers"
        Title="ClipHive Settings"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="True"
        Background="#F8F8F8">

    <Window.Resources>
        <helpers:AutoClearPolicyConverter x:Key="AutoClearConverter" />

        <!-- Light-theme overrides for this window only -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#1A1A1A" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize"   Value="13" />
            <Setter Property="Padding"    Value="0" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1A1A1A" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize"   Value="13" />
        </Style>
        <Style x:Key="SettingsTextBox" TargetType="TextBox">
            <Setter Property="Background"      Value="#FFFFFF" />
            <Setter Property="Foreground"      Value="#1A1A1A" />
            <Setter Property="BorderBrush"     Value="#CCCCCC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding"         Value="8,6" />
            <Setter Property="FontFamily"      Value="Segoe UI" />
            <Setter Property="FontSize"        Value="13" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background"      Value="#FFFFFF" />
            <Setter Property="Foreground"      Value="#1A1A1A" />
            <Setter Property="BorderBrush"     Value="#CCCCCC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily"      Value="Segoe UI" />
            <Setter Property="FontSize"        Value="13" />
            <Setter Property="Padding"         Value="6,4" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#1A1A1A" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize"   Value="13" />
        </Style>
        <Style TargetType="Separator">
            <Setter Property="Background" Value="#E0E0E0" />
            <Setter Property="Margin"     Value="0,4" />
        </Style>
        <Style x:Key="LightButton" TargetType="Button">
            <Setter Property="Background"      Value="#EEEEEE" />
            <Setter Property="Foreground"      Value="#1A1A1A" />
            <Setter Property="BorderBrush"     Value="#CCCCCC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding"         Value="16,6" />
            <Setter Property="FontFamily"      Value="Segoe UI" />
            <Setter Property="FontSize"        Value="13" />
            <Setter Property="Cursor"          Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#DDDDDD" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#CCCCCC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource LightButton}">
            <Setter Property="Background"  Value="#6C72CB" />
            <Setter Property="Foreground"  Value="White" />
            <Setter Property="BorderBrush" Value="#6C72CB" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="ClipHive Settings"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#1A1A1A"
                   Margin="0,0,0,12" />

        <Separator Grid.Row="1" />

        <!-- Form -->
        <Grid Grid.Row="2" Margin="0,12,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Hotkey -->
            <Label Grid.Row="0" Grid.Column="0" Content="Hotkey:" VerticalAlignment="Center" Margin="0,0,0,10" />
            <TextBox x:Name="HotkeyBox"
                     Grid.Row="0" Grid.Column="1"
                     Style="{StaticResource SettingsTextBox}"
                     Text="{Binding HotkeyDisplay, Mode=TwoWay}"
                     IsReadOnly="True"
                     Cursor="Hand"
                     ToolTip="Click and press a key combination"
                     Margin="0,0,0,10"
                     PreviewKeyDown="HotkeyBox_PreviewKeyDown"
                     GotFocus="HotkeyBox_GotFocus"
                     LostFocus="HotkeyBox_LostFocus" />

            <!-- Auto-clear -->
            <Label Grid.Row="1" Grid.Column="0" Content="Auto-clear:" VerticalAlignment="Center" Margin="0,0,0,10" />
            <ComboBox Grid.Row="1" Grid.Column="1"
                      ItemsSource="{Binding AutoClearPolicies}"
                      SelectedItem="{Binding AutoClearPolicy, Mode=TwoWay}"
                      Margin="0,0,0,10">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource AutoClearConverter}}"
                                   Foreground="#1A1A1A" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <!-- Start with Windows -->
            <Label Grid.Row="2" Grid.Column="0" Content="Start with Windows:" VerticalAlignment="Center" Margin="0,0,0,10" />
            <CheckBox Grid.Row="2" Grid.Column="1"
                      IsChecked="{Binding StartWithWindows, Mode=TwoWay}"
                      VerticalAlignment="Center"
                      Margin="0,0,0,10" />

            <!-- Hide from tray -->
            <Label Grid.Row="3" Grid.Column="0" Content="Hide from tray:" VerticalAlignment="Center" Margin="0,0,0,10" />
            <CheckBox Grid.Row="3" Grid.Column="1"
                      IsChecked="{Binding HideFromTray, Mode=TwoWay}"
                      VerticalAlignment="Center"
                      Margin="0,0,0,10" />

            <!-- History limit -->
            <Label Grid.Row="4" Grid.Column="0" Content="History limit:" VerticalAlignment="Center" />
            <TextBox Grid.Row="4" Grid.Column="1"
                     Style="{StaticResource SettingsTextBox}"
                     Text="{Binding MaxHistoryCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <Separator Grid.Row="3" />

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource LightButton}"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}" />
            <Button Content="Save"
                    Style="{StaticResource AccentButton}"
                    Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</Window>
